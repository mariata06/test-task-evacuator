mixin header(headerMods, currentUrl)
  -
    const headerLinks = [
      //- {
      //-   href: "index.html",
      //-   text: "Главная"
      //- },
      {
        href: "#",
        text: "о проекте"
      },
      {
        href: "#",
        text: "контакты"
      },
    ]

  header.header(class=headerMods)
    .container
      if(currentUrl === "index.html")
        //- lighthouse снимает 10 баллов SEO за <a> без href
        span.header__logo
          img(src="img/content/logo-truck.png" alt="truck")
      else
        a.header__logo(href="index.html", aria-label="Переход на главную") Логотип
      nav.main-nav
        button.main-nav__toggle.btn-reset(type="button", aria-label="Переключатель отображения меню", aria-pressed="false")
          +icon("icon-burger", 20, 20)
        .main-nav__wrapper
          ul.main-nav__list
            each link in headerLinks
              - const activeState = currentUrl === link.href
              li.main-nav__item
                if(activeState)
                  span.main-nav__link.is-active= link.text
                else
                  a.main-nav__link.link(href=link.href)= link.text
      button.header__btn.btn.btn--blue-bg(type="button", aria-label="post an ad", aria-pressed="false") Добавить объявление

    .header__phones.phones
      .container
        h2.title.title--h2 Телефоны для вызова эвакуатора в
          a.phones__link.link(href="#", aria-label="телефоны здесь") Москве
